# Anthropic (key provided with the assignment) — used when --provider anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini — used when --provider gemini (https://aistudio.google.com/apikey)
GEMINI_API_KEY=AIza...

# Default provider when --provider is not passed: anthropic | gemini
CURRICULUM_PROVIDER=anthropic
